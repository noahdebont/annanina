import React from 'react'

const Flowbox = ({page}) => {
    if (!page) return null;
    return (
        <div className="flowbox">
            TODO FLOWBOX
        </div>
    )
}

export default Flowbox;
